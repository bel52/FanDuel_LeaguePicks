version: "3.9"
<<<<<<< HEAD

services:
  web:
    build: .
    image: fanduel_web
    ports:
      - "8010:8000"
    env_file:
      - .env
    depends_on:
      - redis
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs

  redis:
    image: redis:7-alpine
    command: ["redis-server", "--appendonly", "yes"]
    # no host port published to avoid conflicts; app talks to 'redis' service over the network
=======
services:
  web:
    build: .
    container_name: dfs-web
    env_file:
      - .env
    environment:
      REDIS_URL: ${REDIS_URL:-redis://redis:6379}
    volumes:
      - ./data:/app/data
    ports:
      - "8010:8000"    # host:container
    depends_on:
      - redis
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: dfs-redis
    # no host port mapping; internal-only
    restart: unless-stopped
>>>>>>> 2577e19 (v1.0 (production): late-swap locks/bans, readable CLI & /optimize_text, structured AI analysis, docs)
